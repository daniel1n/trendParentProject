spring:
  application:
    name: third-part-index-data-project
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP
      discovery:
        server-addr: localhost:8848
