spring:
  application:
    name: index-data-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP
      discovery:
        server-addr: localhost:8848
