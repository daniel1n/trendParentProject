spring:
  application:
    name: index-data-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  zipkin:
    base-url: http://localhost:9411

# Dubbo Configuration
dubbo:
  application:
    name: ${spring.application.name}
  protocol:
    name: tri
    port: 20882
  registry:
    address: nacos://localhost:8848
  scan:
    base-packages: cn.how2j.trend.service.impl
